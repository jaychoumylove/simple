import React from "react";
import Loading from "../component/Loading";
// import "./Juejin.less";

interface ownProps {}

const LoadingPage = (props: ownProps): JSX.Element => {
  return <Loading />;
};

export default LoadingPage;
